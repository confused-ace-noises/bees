[package]
name = "bees"
version = "0.1.0"
edition = "2024"

[dependencies]
bees-core = { path = "../bees-core" }
bees-macros = { path = "../bees-macros", optional = true }
[features]
derive = ["bees-macros"]